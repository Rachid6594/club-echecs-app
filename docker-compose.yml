services:
  backend:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ./backend:/app
    command: ["python", "-m", "http.server", "8000"]
    ports:
      - "8000:8000"
    env_file:
      - .env.example

